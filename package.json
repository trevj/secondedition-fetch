{
  "name": "secondedition-fetch",
  "version": "0.0.1",
  "license": "MIT",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": [
    "build/**/*",
    "dist/**/*"
  ],
  "scripts": {
    "clean": "rm -fR node_modules/ build/ dist/",
    "build": "tsc",
    "dist": "yarn build && webpack",
    "format": "prettier --write 'src/**/*.{js,ts}'"
  },
  "dependencies": {
    "underscore": "^1.13.1"
  },
  "devDependencies": {
    "@types/underscore": "^1.11.1",
    "prettier": "^2.2.1",
    "typescript": "^4.2.4",
    "webpack": "^5.34.0",
    "webpack-cli": "^4.6.0"
  }
}
